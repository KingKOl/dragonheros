
var minSpeed:float=0.7;
var maxSpeed:float=1.5;

function Start () {
    var _animation:Animation;
    _animation = GetComponent("Animation");
    //_animation[_animation.clip.name].speed = Random.Range(minSpeed, maxSpeed);
}